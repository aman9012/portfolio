import type { ExperienceEntry } from "@/types/experience";

/**
 * Experience entries will be added with verified professional history.
 * Placeholder structure only — no fabricated employment data.
 */
export const experienceEntries: ExperienceEntry[] = [];
